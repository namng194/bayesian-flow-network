# =============================================================================
# BFN Seminar - Unified Requirements
# Installs on top of the base conda environment (bfn)
# =============================================================================

# --- Data handling & Math ---
numpy>=1.21.0
pandas>=1.3.0
einops>=0.4.1       # Often required by modern diffusion models (nanoDD/DDPM)

# --- Visualization & Plots (Exp 2d, Exp 3, Exp_final_plots) ---
matplotlib>=3.5.0
seaborn>=0.11.2

# --- Media Generation (For the "Living Demo" - GIF & MP4) ---
imageio>=2.15.0     # Required to compile Theta_hat grids into .gif
imageio-ffmpeg>=0.4 # Required for .mp4 generation (Text8 simplex heatmap)
opencv-python-headless>=4.5.5 # Fast image processing operations

# --- Baselines & Utilities ---
diffusers>=0.19.0   # Useful for baseline DDIM scheduling comparisons
tqdm>=4.62.3        # Progress bars
neptune>=1.0.0      # (Optional) Cloud experiment tracking
omegaconf>=2.1.3    # Configuration management (used by BFN/nanoDD)
rich>=12.0.0        # Pretty terminal formattingsss